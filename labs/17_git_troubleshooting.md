## Removing a branch:
```
# To remove a local branch from your machine:
git branch -d {local_branch} (use -D instead to force deleting the branch without checking merged status);

# To remove a remote branch from the server:
git push origin -d {remote_branch}
```
